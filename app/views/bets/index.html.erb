<h1 class="text-center m-3">Mes paris !</h1>

<div class="d-flex justify-content-between text-center">

  <% unless @ultims.empty?%>
    <div class="card-category m-3">
      <h2><strong>Ultim</strong></h2>
      <% @ultims.each do |ultim| %>
        <p><%= ultim.position%>. <em><%= ultim.boat.name %></em> - <%= ultim.boat.skipper_last_name %> <%= ultim.boat.skipper_first_name %></p>
      <% end %>
    </div>
  <% else %>
    <div class="card-category d-flex align-items-center">
      <%= link_to new_bet_path(category: "Ultim") do %>
        <div class="card-image m-3 d-flex align-items-center justify-content-center" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%=image_url("DSC05013_300x400.jpg")%>')">
          <h2><%= "Ultim" %></h2>
        </div>
      <% end %>
    </div>
  <% end %>

  <% unless @imocas.empty? %>
    <div class="card-category m-3">
      <h2><strong>IMOCA</strong></h2>
      <% @imocas.each do |imoca| %>
        <p><%= imoca.position%>. <em><%= imoca.boat.name %></em> - <%= imoca.boat.skipper_last_name %> <%= imoca.boat.skipper_first_name %></p>
      <% end %>
    </div>
  <% else %>
    <div class="card-category d-flex align-items-center">
      <%= link_to new_bet_path(category: "IMOCA") do %>
        <div class="card-image m-3 d-flex align-items-center justify-content-center" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%=image_url("DSC05125_300x400.jpg")%>')">
          <h2><%= "IMOCA" %></h2>
        </div>
      <% end %>
    </div>
  <% end %>

  <% unless @ocean_fifties.empty? %>
    <div class="card-category m-3">
      <h2><strong>Ocean Fifty</strong></h2>
      <% @ocean_fifties.each do |ocean_fifty| %>
        <p><%= ocean_fifty.position%>. <em><%= ocean_fifty.boat.name %></em> - <%= ocean_fifty.boat.skipper_last_name %> <%= ocean_fifty.boat.skipper_first_name %></p>
      <% end %>
    </div>
  <% else %>
    <div class="card-category d-flex align-items-center">
      <%= link_to new_bet_path(category: "Ocean Fifty") do %>
        <div class="card-image m-3 d-flex align-items-center justify-content-center" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%=image_url("DSC05145_300x400.jpg")%>')">
          <h2><%= "Ocean Fifty" %></h2>
        </div>
      <% end %>
    </div>
  <% end %>

</div>

<div class="d-flex justify-content-between text-center">
  <% unless @class_40s.empty? %>
    <div class="card-category m-3">
      <h2><strong>Class 40</strong></h2>
      <% @class_40s.each do |class_40| %>
        <p><%= class_40.position%>. <em><%= class_40.boat.name%></em> - <%= class_40.boat.skipper_last_name %> <%= class_40.boat.skipper_first_name %></p>
      <% end %>
    </div>
  <% else %>
    <div class="card-category d-flex align-items-center">
      <%= link_to new_bet_path(category: "Class 40") do %>
        <div class="card-image m-3 d-flex align-items-center justify-content-center" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%=image_url("DSC05139_300x400.jpg")%>')">
          <h2><%= "Class 40" %></h2>
        </div>
      <% end %>
    </div>
  <% end %>

  <% unless @rhum_monos.empty? %>
    <div class="card-category m-3">
      <h2><strong>Rhum Mono</strong></h2>
      <% @rhum_monos.each do |rhum_mono| %>
        <p><%= rhum_mono.position%>. <em><%= rhum_mono.boat.name%></em> - <%= rhum_mono.boat.skipper_last_name %> <%= rhum_mono.boat.skipper_first_name %></p>
      <% end %>
    </div>
  <% else %>
    <div class="card-category d-flex align-items-center">
      <%= link_to new_bet_path(category: "Rhum Mono") do %>
        <div class="card-image m-3 d-flex align-items-center justify-content-center" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%=image_url("DSC05296_300x400.jpg")%>')">
          <h2><%= "Rhum Mono" %></h2>
        </div>
      <% end %>
    </div>
  <% end %>

  <% unless @rhum_multis.empty? %>
    <div class="card-category m-3">
      <h2><strong>Rhum Multi</strong></h2>
      <% @rhum_multis.each do |rhum_multi| %>
        <p><%= rhum_multi.position%>. <em><%= rhum_multi.boat.name%></em> - <%= rhum_multi.boat.skipper_last_name %> <%= rhum_multi.boat.skipper_first_name %></p>
      <% end %>
    </div>
  <% else %>
    <div class="card-category d-flex align-items-center">
      <%= link_to new_bet_path(category: "Rhum Multi") do %>
        <div class="card-image m-3 d-flex align-items-center justify-content-center" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%=image_url("DSC05092_300x400.jpg")%>')">
          <h2><%= "Rhum Multi" %></h2>
        </div>
      <% end %>
    </div>
  <% end %>
</div>
