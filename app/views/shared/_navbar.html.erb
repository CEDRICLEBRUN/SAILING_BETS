<div class="navbar-top flex justify-between items-center">
  <div class="navbar-top-left">
    <h2><%= link_to "Accueil", root_path %></h2>
  </div>

  <div class="navbar-top-right flex justify-between">
    <% if user_signed_in? %>
      <h2><%= link_to "Paris", bets_path %></h2>
      <h2><%= link_to "Classement", (League.where_am_i(current_user).empty? ? users_dashboard_path : league_path(League.where_am_i(current_user).first)) %></h2>
      <h2><%= link_to "Bateaux", boats_path %></h2>
      <h2><%= link_to "Mon compte", users_dashboard_path %></h2>
    <% else %>
      <h2><%= link_to "Se connecter", new_user_session_path %></h2>
    <% end %>
  </div>
</div>
